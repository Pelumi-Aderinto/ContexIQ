# ContextIQ development tools, pinned to the versions in uv.lock.
# Installs the runtime dependencies too:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.16.8
